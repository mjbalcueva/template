// Tailwind v4: no tailwind config required. This file left intentionally blank to avoid
// accidental scanning or overrides. Tailwind v4 uses the PostCSS plugin and
// reads CSS via the PostCSS pipeline.

module.exports = {}
