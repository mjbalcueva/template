/** @type {import('prettier').Config} */
const config = {
  endOfLine: "lf",
  printWidth: 80,
  semi: false,
  singleQuote: false,
  tabWidth: 2,
  trailingComma: "none",
  useTabs: true,
};

export default config;
